//Design the logic to remove the duplicate elements(array doesn't have to be sorted) from an Array and after the deletion the array should contain the unique elements.
package Arrays;

public class RemoveDuplicates
{
    public static void Remove(int[] arr, int size)
    {
        if(size == 0)
        {
            System.out.println("Array is empty!");
            return;
        }
        
    }

        
